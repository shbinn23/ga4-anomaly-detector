# ── FastAPI / Prophet ──────────────────────────────────────────────
PROJECT_NAME=GA4 Anomaly Detector

# results_db.json 파일명 (data/ 디렉토리 안에 위치)
DB_FILE_NAME=results_db.json

# Prophet 예측 구간 폭 (0~1, 낮을수록 이상치 탐지 민감도 높아짐)
PROPHET_INTERVAL_WIDTH=0.80

# Priority Watch: 항상 모니터링할 핵심 Property ID (쉼표 구분)
PRIORITY_WATCH_IDS=prop_id1,prop_id2
